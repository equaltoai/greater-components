# Minor — record module CSS in the Registry

Registry entries now enumerate their CSS assets explicitly. Shared suites record the consolidated
Face stylesheet that owns their classes, and the CLI hydrates those verified styles into source
install plans instead of requiring consumers to infer appearance dependencies.
