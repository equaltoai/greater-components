# Patch — complete explicit dark-theme coverage for author cards and menus

Blog author cards and the SimpleMenu surface now provide `data-theme="dark"` colors with WCAG 2.1
AA text contrast. System-preference behavior remains intact, while consumers that explicitly select
Greater's dark theme no longer receive light-only surfaces.

No public token names or semantic meanings changed.
