# Minor — add an accessible shell BottomNav

The Shell package now exports `BottomNav`, a strict-CSP-safe mobile navigation landmark with
safe-area support, current-page styling, and a 44px minimum touch-target floor for direct links and
buttons.
