# Community Face: Group Management

The community face exposes the `Community` compound component for headers, stats, and rules, and the `Post` and `Thread` components for discussion UI.

## Components

```ts
import { Community, Post, Thread } from '@equaltoai/greater-components/faces/community';
```

