# Historical Lesser Host fixtures

`host-v1.0.7/` preserves the released hosted-genesis fixture payloads used by
Project 49 representability tests. These are intentionally historical fixtures,
not the active Lesser Host v1.0.15 contract fixtures. Keep them separate so
contract-sync updates do not erase legacy representability coverage.
