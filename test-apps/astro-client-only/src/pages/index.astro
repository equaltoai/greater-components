---
import '@equaltoai/greater-components-tokens/theme.css';
import '@equaltoai/greater-components-primitives/style.css';
import PrimitivesTest from '../components/PrimitivesTest.svelte';
import HeadlessTest from '../components/HeadlessTest.svelte';

const buildTime = new Date().toISOString();
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Greater Components - Client-Only Test</title>
    <style>
      * {
        box-sizing: border-box;
      }
      
      body {
        margin: 0;
        padding: 0;
        background: linear-gradient(to bottom, #f9fafb, #e5e7eb);
        min-height: 100vh;
      }
      
      .build-info {
        background: #1f2937;
        color: white;
        padding: 1rem 2rem;
        font-family: monospace;
        font-size: 0.875rem;
      }
      
      .build-info strong {
        color: #34d399;
      }
      
      .notice {
        background: #fef3c7;
        border-left: 4px solid #f59e0b;
        padding: 1.5rem;
        margin: 2rem;
        border-radius: 0.5rem;
      }
      
      .notice h2 {
        margin-top: 0;
        color: #92400e;
      }
      
      .notice p {
        margin-bottom: 0;
        color: #78350f;
      }
    </style>
  </head>
  <body>
    <div class="build-info">
      <strong>BUILD MODE:</strong> Static (no SSR) | 
      <strong>BUILT AT:</strong> {buildTime} | 
      <strong>DEPLOYMENT:</strong> S3/CloudFront Ready
    </div>
    
    <div class="notice">
      <h2>🎯 IMPORTANT: This page uses client:only</h2>
      <p>
        All Svelte components below are hydrated <strong>client-side only</strong>.
        No server-side rendering is involved. This proves that Greater Components
        work perfectly for static deployments to S3/CloudFront.
      </p>
    </div>
    
    <!-- Primitives Test - client:only means it only runs in the browser -->
    <PrimitivesTest client:only="svelte" />
    
    <!-- Headless Test - also client:only -->
    <HeadlessTest client:only="svelte" />
    
    <div style="max-width: 800px; margin: 2rem auto; padding: 0 2rem;">
      <div style="background: #dcfce7; border: 2px solid #16a34a; border-radius: 0.5rem; padding: 1.5rem;">
        <h2 style="margin-top: 0; color: #166534;">✅ Success!</h2>
        <p style="color: #166534; margin-bottom: 0;">
          If you can see and interact with the components above, it proves that
          <strong>Greater Components work perfectly with client-only rendering</strong>.
          They do NOT require SSR.
        </p>
      </div>
    </div>
    
    <script>
      // Client-side JavaScript to prove it's running in the browser
      console.log('%c✅ RUNNING IN BROWSER', 'color: green; font-size: 16px; font-weight: bold;');
      console.log('%cThis page was built statically and all components are hydrated client-side.', 'color: blue; font-size: 14px;');
      console.log('%cGreater Components work perfectly without SSR!', 'color: purple; font-size: 14px;');
      
      // Add runtime info to page
      const runtimeInfo = document.createElement('div');
      runtimeInfo.style.cssText = 'position: fixed; bottom: 0; right: 0; background: #1f2937; color: #34d399; padding: 0.5rem 1rem; font-family: monospace; font-size: 0.75rem; border-top-left-radius: 0.5rem;';
      runtimeInfo.textContent = `✅ Client-side JS loaded at ${new Date().toLocaleTimeString()}`;
      document.body.appendChild(runtimeInfo);
    </script>
  </body>
</html>

