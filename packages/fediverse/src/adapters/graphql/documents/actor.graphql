# Actor lookup queries

#import "./fragments.graphql"

query ActorById($id: ID) {
  actor(id: $id) {
    ...ActorSummary
  }
}

query ActorByUsername($username: String) {
  actor(username: $username) {
    ...ActorSummary
  }
}
