import { describe, it, expect } from 'vitest';

describe('testing package vitest pipeline', () => {
  it('confirms vitest runs when Playwright suites are excluded', () => {
    expect(true).toBe(true);
  });
});



