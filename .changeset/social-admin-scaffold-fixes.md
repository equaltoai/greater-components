---
'greater-components': patch
---

Fix `greater add social` scaffolding gaps (including virtualized timelines), and add missing interaction/admin workflow building blocks needed for real-world instance frontends.

