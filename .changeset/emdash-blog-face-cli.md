---
"@equaltoai/greater-components-cli": patch
"@equaltoai/greater-components": patch
---

Fix Blog face CLI installs so shallow Article components materialize the complete display surface, use Blog-scoped helper/types files, keep fresh installs clean under `greater diff`, and include vendored adapter runtime dependencies such as `viem`.
